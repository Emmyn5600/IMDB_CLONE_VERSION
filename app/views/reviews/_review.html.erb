<p>
    <span>Rating:
        <%  review.rating.to_i.times do  %>
            <%= image_tag 'star-on.png', style: 'height:7%; width:7%;' %>
        <% end %>
    </p>
</span></p><p>
<span>Comment:
    <%= review.comment %></span></p><%if user_signed_in? %>

<%if review.user_id == current_user.id%>

    <%= link_to "Edit", edit_movie_review_path(review.movie, review), class: "btn btn-outline-primary mb-2"%>
    <%= link_to "Delete", movie_review_path(review.movie, review), method: :delete, data:{confirm: "Are you sure!?"}, class: "btn btn-outline-danger mb-2"%>
    <%end%>
    <%end%>
